# 2026-08-26 必要终止边界聚焦 SFT 与双向 DPO

## 今天完成了什么

1. 定位到上一轮 SFT 课程中 `abort` 为 0；加入修复 shard 后，`abort` 在 1,224 条训练样本中仍只有 48 条，信号不足。
2. 构建并训练全能力混合修复 SFT，保存 40/80/120/160/200/240 六个检查点；快速边界结果始终为协商 8/8、终止 2/8，拒绝晋级。
3. 新增聚焦课程构建器，按 `abort:propose_tradeoff:正常回放=1:1:1` 构建 156/27/45 的 SFT 数据，并完成三轮低学习率训练。
4. 聚焦 SFT 在 80 步达到本轮最佳：协商 8/8、终止 5/8；未达到终止至少 7/8 的快速门槛。
5. 发现通用 reward verifier 无法识别“有替代方案却直接 abort”的错误，补充可执行的双向决策边界偏好合同和 DPO 数据门禁。
6. 构建 152 对双向偏好数据并训练 DPO。离线验证偏好准确率 100%，真实 Agent Loop 为协商 8/8、终止 4/8，拒绝晋级。
7. 云端相关单测 10/10 通过，本地 Ruff 通过；数据、日志与原始评测结果已归档回本地。

## 当前状态

- 当前冠军模型不变。
- 新 SFT/DPO 均为失败实验，不部署、不下载权重、不对外宣称提升。
- 已确认有效进展：聚焦 SFT 把必要终止快速集从长期低位推到最高 5/8，同时未破坏协商 8/8。
- 尚未解决：自由生成时 `abort` 与 `propose_tradeoff` 的稳定边界。

## 下一步

先修改通用 reward verifier，让它依据 `capability.actionable_alternatives` 同时惩罚两种边界错误；补齐 verifier 单测后，再做小规模 on-policy GRPO/拒绝采样。只有快速边界达到协商 8/8、终止至少 7/8，才运行完整 32 题、多随机种子和旧搜索/恢复能力回归。

详细证据见 `ml/agentic/reports/stage2-decision-boundary-repair-v3/README.md`。
