# 阶段 26：仓库与证据冻结审计工作日志

> 日期：2026-08-15  
> 对应 SPEC：`TravelAgent_大厂级后训练与在线验证升级_SPEC_v1.0.md`  
> 当前状态：Blocked（证据本身通过；发布安全与 Git 清洁门未通过）

## 1. 本阶段目标

在进入 External Benchmark 和 Qwen3 RL 前，冻结阶段 25 的可信基线，并确认第三方能够：

1. 精确重建阶段 24/25 摘要；
2. 校验模型、数据、报告和 release archive 的 SHA-256；
3. 确认候选发布内容不含凭据；
4. 将内部工程 benchmark 与后续 external sealed test 明确区分；
5. 在 Git 状态清洁后创建 release candidate。

## 2. 新增实现

新增 `scripts/build_stage26_evidence_freeze.py`，提供一键审计：

- 固定核对阶段 25 的 6 个 headline 指标；
- 从冻结源文件重新构建 Stage 24/25 JSON，并做 canonical SHA-256 精确比较；
- 对 16 个模型、数据、报告和归档产物生成 SHA-256 清单；
- 扫描当前候选发布树中的高置信凭据模式；
- 扫描所有 Git 历史中的高置信凭据模式，只保存 rule、commit 数和路径，不保存命中值；
- 只记录 Git 修改数量，不把路径或 diff 内容复制进冻结报告；
- 任一门禁不通过时返回非零退出码，避免误创建 release candidate。

新增 `backend/tests/unit/agentic/test_stage26_evidence_freeze.py`，覆盖：

- Stage 24/25 双报告指标一致性；
- Git 状态聚合不泄漏路径；
- 当前树 secret scan 只输出脱敏结果；
- Git 历史扫描结果解析。

生成：

- `ml/agentic/reports/stage26-evidence-freeze-v1/report.json`
- `ml/agentic/reports/stage26-evidence-freeze-v1/REPORT.md`

## 3. 审计结果

| 门禁 | 结果 | 说明 |
|---|---|---|
| 冻结基线一致 | PASS | 516/516、172/172、Reward 0.965842 等指标无漂移 |
| Stage 24/25 摘要精确重建 | PASS | canonical JSON 完全一致 |
| 产物哈希清单完整 | PASS | 16/16 个冻结产物存在并完成 SHA-256 |
| 当前发布树无高置信凭据 | PASS | 工作区中的疑似真实值已替换为占位符 |
| Git 历史无高置信凭据 | BLOCKED | `.env.example` 在 1 个历史 commit 中存在凭据形态命中 |
| Git 工作区清洁 | BLOCKED | 当前分支存在大量前序阶段尚未收口的修改和新增文件 |

因此 Stage 26 总状态必须保持 `blocked`，当前不能诚实地创建 release tag，也不能把仓库直接公开。

## 4. 凭据处置

审计在 `.env.example` 发现疑似真实的 DeepSeek/OpenAI-compatible API key：

- 工作区版本已经替换为 `replace-with-deepseek-api-key`；
- 测试中的模拟 token 改为运行时拼接，避免扫描器自我命中；
- 报告、测试输出和本日志均未记录原始值；
- Git 历史仍有 1 个 commit 命中，所以必须将对应密钥视为已暴露并在服务商后台轮换；
- 在用户明确授权前，不自动执行历史重写、强制推送或批量提交。

## 5. 验证

执行：

```powershell
.\backend\.venv\Scripts\python.exe -m pytest `
  backend/tests/unit/agentic/test_stage24_final_report.py `
  backend/tests/unit/agentic/test_stage25_showcase.py `
  backend/tests/unit/agentic/test_stage26_evidence_freeze.py -q
```

结果：`8 passed`。

单独执行阶段 26 审计：

```powershell
.\backend\.venv\Scripts\python.exe scripts\build_stage26_evidence_freeze.py
```

结果：返回码 `2`，状态 `blocked`；这是门禁按设计拒绝发布，不是脚本运行失败。

## 6. 下一步

必须先完成：

1. 在 DeepSeek 对应控制台撤销并轮换旧 key；
2. 选择安全发布方式：重写 Git 历史，或从清洁快照建立新的公开仓库；
3. 审阅当前 249 条 Git 状态记录，把阶段 1～26 的成果按可审计提交收口；
4. 重新运行 Stage 26 审计，确保全部门禁 PASS；
5. 创建 Stage 25 release candidate tag 后，再正式封存 Phase 27 的 External Benchmark Test split。

在以上动作完成前，可以继续设计外部评测 schema 和标注指南，但不应生成或查看 400 条 sealed test 的正式内容。

## 7. 后续安全加固

根据用户“真实 API 不上传 Git”的决定，已完成：

- `.gitignore` 改为忽略 `.env.*`，仅允许占位模板 `.env.example`；
- 增加私钥、证书、`secrets/` 和本地 Kubernetes Secret overlay 忽略规则；
- 新增 `scripts/check_staged_secrets.py`，只扫描 Git staged blob，不读取未暂存的本地 `.env`；
- 新增 `.githooks/pre-commit` 并在当前仓库启用 `core.hooksPath=.githooks`；
- 扫描命中只输出路径、行号、规则和 `[REDACTED]`，绝不输出值；
- 安全钩子与 Stage 26 测试共 `7 passed`，实际 hook dry run 通过。

当前 `HEAD` 与远端 `origin/feat/agentic-rl-long-horizon` 相同，因此历史命中已经存在于远端；本轮不重写历史，也不推送任何新提交。后续所有真实 key 只能放在被忽略的本地 `.env` 或部署平台 Secret 中。
