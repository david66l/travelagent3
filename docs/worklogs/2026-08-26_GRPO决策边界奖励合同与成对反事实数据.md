# 2026-08-26 GRPO 决策边界奖励合同与成对反事实数据

## 本轮目标

在继续租用显卡前，修复 necessary abort 与 actionable tradeoff 无奖励差的问题，并生成
可直接用于下一轮 on-policy GRPO 的正式数据。

## 完成事项

- 定位到旧 corpus 的 450 条不可行任务缺少显式决策合同。
- 为 GRPO preflight 增加不可行任务硬门。
- reward 版本升级到 `hierarchical-b0.v2`。
- 将终止 reason/options 的 grounding 纳入训练 reward，阻止空泛参数刷分。
- 增加动作四象限、合同缺失、上下文状态一致性及 reward hacking 测试。
- 新增 `build_decision_boundary_grpo_corpus.py`。
- 构建训练 1,024 条、验证 192 条的 split-safe 成对反事实 corpus。
- 对 912 条边界任务执行正确/相反动作双跑，全部得到预期奖励方向，最小 gap 1.139125。
- 冻结困难集污染审计为 0；第一次发现 4 条候选锚点请求重合后，已在选样阶段排除并重建。
- 完整 Agentic 单测 321/321 通过，Ruff 通过。

## 当前状态

- 数据、reward、preflight 和单测已经具备进入采样诊断的条件。
- 当前冠军保持不变；本轮未训练新模型。
- 暂时不需要显卡，下一步才需要 RTX 4090。

## 下一步

1. 上传新代码、数据和 manifest 到云端。
2. 从 SFT-80 在训练外 64 题上每题采样 8 条轨迹。
3. 统计组内正负奖励覆盖率、abort 探索率、reward/semantic audit 冲突率。
4. 达到 70% 有效组内方差后启动 20/40/60 step 小规模 GRPO；否则先做拒绝采样 RFT。

## 显卡阶段续记

- 温度 1.0 的 64 题 × 8 探索只有 40/64 组有方差（62.5%），未达到门禁。
- 温度 1.2 的 16 题小样为 14/16（87.5%），扩大 64 题正式门禁为 54/64
  （84.38%），且 0 全错组、0 动作错误、0 格式错误。
- 训练入口增加显式 temperature 与 save_steps，并把报告 reward 版本改为读取
  `RewardConfig.config_version`。
- 修复可选 capability `reasons/alternatives=null` 导致 GRPO 环境 reset 崩溃的问题；
  回归 12/12 通过，全部 1,216 条 corpus 逐条状态投影通过。
- 从 SFT-80 完成 20-step GRPO；优化与模型保存成功，报告异常已透明恢复并保留原日志。
- 冻结 32 题 × 4 配对结果：SFT-80 108/128，GRPO-20 107/128；GRPO-20 未晋级。
- 已从 SFT-80 启动 60-step 分段训练，保留 20/40/60 checkpoint，等待逐点验收。

## 60-step 验收与根因收敛

- 60/60 step 正常完成，耗时 1:46:38；checkpoint-40/60 已保存。
- checkpoint-20 曾保存，但被 `save_total_limit=2` 自动清理；训练入口已改为默认保留 3 个。
- 快速冻结边界结果：SFT-80 44/64、step40 43/64、step60 44/64。
- step40/60 的 necessary abort 均为 12/32，低于 SFT-80 的 13/32，全部拒绝。
- 数据审计定位根因：v1 训练 abort 384 条全部为 `infeasible`，冻结 necessary abort
  8 题全部为 `unsafe`，存在明确状态分布缺口。
- 构建 v2 状态均衡 corpus：训练 1,024、验证 192；三类状态各 304 条边界样本，
  actionable/non-actionable 1:1；912/912 奖励矩阵通过；冻结重合 0。

当前停止继续训练旧 v1。下一门禁是 v2 的分状态 on-policy 方差诊断，不通过则继续改
数据与奖励，不再靠增加 step 数碰运气。

当前冠军仍为 SFT-80。不得把 GRPO-20 写成效果提升，只能作为“奖励合同、on-policy
探索、训练与拒绝门禁均已跑通”的工程证据。

## 2026-08-27：状态桥接与 v2 闭环

- 修正 v2 corpus 的 schema/derivation 版本并重建：训练 1,024、验证 192，三类状态
  各 304 条，912/912 正反奖励矩阵通过，冻结重合 0。
- 为 audit 增加按 `boundary_kind / variant` 分层抽样，避免总体方差掩盖局部全错。
- SFT-80 的 v2 分层结果为 107/192；`unsafe/necessary_abort` 仅 1/32，4 组中 3 组
  全错，确认不能直接 GRPO。
- 新增 verifier-labelled status bridge 构建器：正确终止动作必须在真实环境获得正奖励；
  训练 624、验证 72、测试 72，768/768 可见 prompt 唯一，split overlap 0。
- 低剂量 SFT（1e-6、20 step）对 unsafe-abort 无改善，拒绝。
- 强剂量 SFT（3e-6、40 step）把分层总通过从 107/192 提升至 139/192，unsafe-abort
  从 1/32 提升至 19/32，全错组从 3 降为 0，允许进入 GRPO。
- v2 GRPO 从 bridge-40 训练 20 step：checkpoint-10 unsafe-abort 19/32（持平），
  checkpoint-20 18/32（回落），两者均拒绝，不继续加 step。
- 冻结 32 题 × 4：bridge-40 为 117/128；SFT-80 为 108/128。细分为 clarification
  32/32、terminal-injection/search 32/32、actionable tradeoff 32/32、necessary abort
  21/32。当前冠军更新为 `stage2-sft-v5-status-bridge-lr3e6-step40`。
- 证据归档已同步本地：
  `artifacts/remote-backups/stage2-status-bridge-grpo-v2-evidence-20260827.tar.gz`，
  SHA256 `30f92c4cc7c5105c9bde26f4d766838687dabde41f616ebfecbf2d392af81cb6`。

下一阶段不得把被拒绝的 GRPO 写成准确率提升。可表述为：完成状态分层探索门禁、
verifier-labelled SFT 支持域修复、on-policy GRPO 与冻结集拒绝门禁；最终由桥接 SFT
取得 7.03 个百分点绝对提升，RL 试验因未超过起点被自动拒绝。
